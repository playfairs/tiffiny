[package]
name = "tiffiny"
version = "0.1.0"
edition = "2024"
description = "Convert Audio files into Images using TIFF Headers and RAW Data Manipulation."

[dependencies]
clap = { version = "4.5", features = ["derive"] }
image = "0.25"
indicatif = "0.17"
