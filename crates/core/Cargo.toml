[package]
name = "core"
version = "0.1.0"
edition = "2024"
description = "Core functionality for tiffiny audio-to-image converter"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dirs = "5.0"
