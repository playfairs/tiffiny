[package]
name = "cli"
version = "0.1.0"
edition = "2024"
description = "CLI interface for tiffiny"

[[bin]]
name = "tiffiny"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
core = { path = "../core" }
image = { path = "../image" }
